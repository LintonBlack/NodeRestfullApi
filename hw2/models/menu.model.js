/** 
 * 
 * Menu data model
 * 
*/

let menu = {};

// Definition
menu.model = ( ( ) => {
    this.pizzaId = pizzaId;
    this.pizzaName = pizzaName;
    this.ingredients = ingredients;
    this.price = price;
});

// Export the module
module.exports = menu;