/** 
 * 
 * Token data model
 * 
*/

let token = {};

// Definition
token.model = ( (  ) => {
    this.id = id;
    this.phone = phone;
    this.expires = expires;
});

// Export the module
module.exports = token;